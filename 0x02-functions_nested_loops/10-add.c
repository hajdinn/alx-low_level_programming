#include "main.h"
/**
 * add - start function that adds two integers
 * @x: first number to be computed
 * @y: second number to be computed
 * Return: sum of the two integer
 */
int add(int x, int y)
{
	return (x + y);
}
