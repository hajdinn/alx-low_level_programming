#include "main.h"
/**
 * _strlen - start of function that return string length
 * @s: get sring
 * Return: i
 */
int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
