#include <stdio.h>
/**
 * main - starting of program to print both lower an uppercase alphabet
 *
 * Return: 0 (program is a success)
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
		putchar(lowercase a);
	for (a = 'A'; a <= 'Z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
