#include <stdio.h>
/**
 * main - starting of program to print both lower an uppercase alphabet
 *
 * Return: 0 (program is a success)
 */
int main(void)
{
	char lowercase = a;
	char uppercase = A;

	for (lowercase a = 'a'; a <= 'z' ; lowercase a++)
		putchar(lowercase a);
	for (uppercase A = 'A'; a <= 'Z'; uppercase A++)
		putchar(uppercase A);
	putchar('\n');
	return (0);
}
