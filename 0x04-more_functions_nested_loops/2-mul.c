#include "main.h"
#include <stdio.h>
/**
 * mul - multiply of two numbers
 * @a: the first number to multiply
 * @b: the second number to multiply
 * Return: 0
 */
int mul(int a, int b)
{
	return (a * b);
}
